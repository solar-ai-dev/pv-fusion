# Thermal Models

이 디렉터리는 thermal 모델 manifest 예시를 보관합니다.

- 실제 ONNX 파일은 Git에 포함하지 않습니다.
- 운영에서는 S3 artifact에서 `/models/thermal/stage10b/v20260704-r1`로 다운로드한 뒤 사용합니다.
- `stage10b/v20260704-r1/model-manifest.yaml`의 `model_path`는 같은 디렉터리의 상대 파일명만 가리켜야 합니다.
