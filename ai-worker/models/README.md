# AI Worker Local Models

이 디렉터리는 로컬 ONNX smoke 검증용 모델 파일 배치 위치입니다.

- 실제 `.onnx`, `.pt`, `.engine`, `model-manifest*.yaml`, 모델별 `README.md`는 Git에 커밋하지 않습니다.
- 로컬 개발 환경에서는 `rgb/`, `thermal/` 하위에 모델 파일을 두고 `.env` 또는 환경변수로 경로를 주입합니다.
- 이 저장소에는 공통 안내용 `README.md`와 `.gitkeep`만 추적합니다.
